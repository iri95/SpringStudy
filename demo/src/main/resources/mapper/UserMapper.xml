<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.dao.UserDao">
	<select id="login" parameterType="com.example.demo.dto.UserDto" resultType="com.example.demo.dto.UserDto">
		select *
		  from user
		 where user_id = #{userId} and user_pw = #{userPw}
	</select>
	
	<insert id="signin" parameterType="com.example.demo.dto.UserDto">
		insert into user (user_id, user_pw, user_name, email, phone, address, date)
		values (#{userId}, #{userPw}, #{userName}, #{email}, #{phone}, #{address}, now())
	</insert>
	
</mapper>